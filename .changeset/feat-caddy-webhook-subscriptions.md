---
---

feat(caddy): add webhook subscription support

Adds webhook subscription handling to the private Caddy plugin, including
callback tokens, webhook signing, JWKS discovery, matching, retry, and callback
routes. This is an empty changeset because the Caddy package is private and not
published by Changesets.
