---
'@durable-streams/cli': patch
---

Add `--url` flag to override STREAM_URL environment variable and `--help`/`-h` flags for usage information. Improve validation with helpful error messages for URLs, auth headers, and stream IDs. Enhance success/error messages with better formatting including byte counts and HTTP status codes.
